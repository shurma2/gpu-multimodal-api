# Copy to .env and adjust. `docker compose` reads .env automatically.

# ---- auth ----------------------------------------------------------------- #
# Public API key (Authorization: Bearer <key>). Leave empty to disable auth.
API_KEY=change-me

# ---- Hugging Face --------------------------------------------------------- #
# Gemma is a gated model: accept the license on its HF page and set a token.
# https://huggingface.co/google/gemma-4-12B-it-qat-q4_0-gguf
HF_TOKEN=

# ---- LLM (Gemma 4 via llama.cpp) ------------------------------------------ #
LLM_HF_REPO=google/gemma-4-12B-it-qat-q4_0-gguf
LLM_HF_QUANT=Q4_0
LLM_MODEL_NAME=gemma-4-12b
LLM_GPU_LAYERS=99          # 99 = offload everything; lower to spill to CPU/RAM
LLM_CONTEXT=8192           # drop to 4096 on 12 GB cards to save VRAM
LLM_PARALLEL=2             # concurrent request slots
# Perf/VRAM (recent llama.cpp wants a value for --flash-attn: on|off|auto).
# LLM_PERF_ARGS=--flash-attn on --cache-type-k q8_0 --cache-type-v q8_0
# LLM_EXTRA_ARGS=

# ---- TTS (Kokoro-82M) ----------------------------------------------------- #
TTS_MODEL_NAME=kokoro
TTS_DEFAULT_SPEAKER=am_michael   # english voices: am_michael am_adam bm_george af_heart bf_emma
TTS_LANG_CODE=a                  # a = American English, b = British English
TTS_DEFAULT_LANGUAGE=English
TTS_DEVICE=cuda
TTS_SPEED=1.0                    # 0.5..2.0 speech rate

# ---- gateway -------------------------------------------------------------- #
GATEWAY_TIMEOUT=600
